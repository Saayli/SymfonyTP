{% extends 'base.html.twig' %}

{% block title %}Supprimer la catégorie{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Etes-vous sûr de vouloir supprimer la catégorie ?<i>{{ categorie.titre }}</i></h1>
        {{ form(formulaire) }}
        <a href="{{ path("app_categories") }}" class="btn btn-warning">Annuler</a>
    </div>

{% endblock %}